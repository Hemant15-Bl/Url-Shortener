spring:
  application:
    name: URL-SERVICE

  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
#      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false

  data:
    mongodb:
      uri: mongodb://admin:qwerty@localhost:27017
      database: spring_micro
      username: admin
      password: qwerty

    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: 6379

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 200566460056-qlkqa2crgiottbur2dg8k5geq2fjn04q.apps.googleusercontent.com
            client-secret: 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
  
server:
  port: 9094

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

google:
  api:
    key: AIzaSyCa5nFNVYo4DqkCrP_bacMzc5ncEBC0ZKE

internal:
  secret: my-super-secret-key

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always