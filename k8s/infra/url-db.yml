apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: url-db-pvc
  namespace: default
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: url-db
  namespace: default
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  replicas: 1
  selector:
      matchLabels:
        app: url-db
  template:
    metadata:
      labels:
        app: url-db
    spec:
      containers:
        - name: url-db
          image: mongo:latest
          args:
            - "--wiredTigerCacheSizeGB=0.25" # Increased to Mongo's hard minimum floor
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_DATABASE
              value: url_DB
            - name: MONGO_INITDB_ROOT_USERNAME
              value: admin
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: qwerty
          resources:
            limits:
              memory: "64Mi"  # Increased to give ~256Mi headspace above the WT cache
              cpu: "20m"
            requests:
              memory: "384Mi" # Bumped up to safely cover the initial 0.25GB cache startup
              cpu: "300m"
          # NATIVE MONGO HEALTH CHECK
          livenessProbe:
            tcpSocket:
              port: 27017
            exec:
              command:
                - mongosh
                - --eval
                - "db.adminCommand({ ping: 1 })"
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 4
          readinessProbe:
            tcpSocket:
              port: 27017
            exec:
              command:
                - mongosh
                - --eval
                - "db.adminCommand({ ping: 1 })"
            initialDelaySeconds: 40
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 4
          volumeMounts:
            - name: url-db-storage
              mountPath: /data/db
      volumes:
        - name: url-db-storage
          persistentVolumeClaim:
            claimName: url-db-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: url-db
  namespace: default
spec:
  selector:
    app: url-db
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP